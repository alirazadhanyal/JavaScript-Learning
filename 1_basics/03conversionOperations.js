let name = "raza"
let age = "32"
let gpa = 3.4
let isPass = true

console.table([name, Number(age),String(gpa),isPass])


/*
    Thats how we convert data types with the help of Conversion Operations.
    eg. "At first age was String but when we convert age 
    in to the number the output is number"
*/
